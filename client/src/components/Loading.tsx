import './Loading.scss';

const Loading = () => (
    <section className="loading">
        <div className="container-max loading-content">
            <span className="loading-text">Loading, please wait...</span>
        </div>
    </section>
)

export default Loading;